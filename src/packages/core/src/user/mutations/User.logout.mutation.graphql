mutation logout($userSessionId: UserSessionID!) {
    usersLogout(input: {
        userSessionId: $userSessionId
    }) {
        errors {
            ...on ActiveModelError { attribute type }
            ...on MessageError { message }
        }
        userSession {
            id
        }
    }
}