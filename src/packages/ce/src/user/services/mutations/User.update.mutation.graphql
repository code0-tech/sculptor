#import "@edition/user/services/fragments/User.basic.fragment.graphql"
mutation update($userId: UserID!, $email: String, $username: String, $firstname: String, $lastname: String, $readme: String, $admin: Boolean, $blocked: Boolean, $password: String, $passwordRepeat: String, $mfa: MfaInput) {
    usersUpdate(input: {
        userId: $userId
        email: $email
        username: $username
        firstname: $firstname
        lastname: $lastname
        readme: $readme
        admin: $admin
        blocked: $blocked
        password: $password
        passwordRepeat: $passwordRepeat
        mfa: $mfa
    }) {
        errors {
            ...on Error {
                errorCode,
                details {
                    __typename
                }
            }
        }
        user {
            ...UserBasic
        }
    }
}
