mutation delete($userId: UserID!) {
    usersDelete(input: {
        userId: $userId
    }) {
        errors {
            ...on Error {
                errorCode,
                details {
                    __typename
                }
            }
        }
        user {
            id
        }
    }
}