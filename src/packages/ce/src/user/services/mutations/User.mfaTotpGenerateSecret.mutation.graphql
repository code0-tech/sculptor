mutation mfaTotpGenerateSecret {
    usersMfaTotpGenerateSecret(input: {}) {
        errors {
            ...on Error {
                errorCode,
                details {
                    __typename
                }
            }
        }
        secret
        signedSecret
    }
}
