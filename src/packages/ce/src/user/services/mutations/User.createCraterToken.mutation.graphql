mutation createCraterToken {
    usersCreateCraterToken(input: {}) {
        errors {
            ...on Error {
                errorCode,
                details {
                    __typename
                }
            }
        }
        token {
            token
        }
    }
}
