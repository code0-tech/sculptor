mutation userPasswordResetRequest($email: String!) {
    usersPasswordResetRequest(input: {
        email: $email
    }) {
        errors {
            ...on Error {
                errorCode,
                details {
                    __typename
                }
            }
        }
        message
    }
}