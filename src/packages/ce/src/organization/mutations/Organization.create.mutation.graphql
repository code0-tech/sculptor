#import '../fragments/Organization.fragment.graphql'
mutation createOrganization ($name: String!) {
    organizationsCreate(input: {
        name: $name
    }) {
        errors {
            ...on Error {
                errorCode,
                details {
                    __typename
                }
            }
        }
        organization {
            ...Organization
        }
    }
}